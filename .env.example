# Application
APP_ENV=production
APP_URL=https://crm.domain.com

# Database
DB_HOST=localhost
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASS=your_db_password

# Session (optional, PHP defaults work fine on cPanel)
SESSION_NAME=dhf_session
SESSION_LIFETIME=7200
